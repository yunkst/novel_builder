### ♻️ 重构
- **Agent 对话「运行中」机制统一**：发送/重试/回滚/清空/切会话时若 Agent 仍在跑，统一先停止当前回合再执行新操作（interrupt-then-act），不再弹"正在运行中"拒绝提示
- 修复切会话时老 Agent 的流式输出可能写进新会话、导致对话串台的数据污染问题
- 切换场景保持原有「不打断后台 Agent」的行为

### ✨ 新功能
- Agent 创作新章节时自动注入上一章正文作为衔接参考，提升前后文连贯性
- 角色工具（创建/更新角色）支持性别、年龄、性格、外貌、别名等结构化字段
- 保存大纲不再强制要求标题，缺省时用书名兜底
