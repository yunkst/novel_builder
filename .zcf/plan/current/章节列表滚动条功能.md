# 章节列表滚动条功能

## 任务上下文

**创建时间**：2026-01-13 23:40:51
**目标**：为章节列表实现原生滚动条，支持拖动快速滚动
**文件**：`novel_app/lib/screens/chapter_list_screen.dart`

## 方案选择

**方案1：使用 Flutter 内置 Scrollbar widget（已采纳）**

- 原生Android/iOS样式
- 零额外依赖
- 实现简单，风险最低

## 执行计划

### 步骤 1：修改 `_buildNormalChapterList()` 方法

**位置**：`chapter_list_screen.dart:799-836`

**操作**：用 `Scrollbar` widget 包裹 `ListView.builder`

```dart
Widget _buildNormalChapterList() {
  return Scrollbar(
    controller: _scrollController,
    child: ListView.builder(
      controller: _scrollController,
      itemCount: _chapters.length,
      itemBuilder: (context, index) {
        // ... 现有代码保持不变
      },
    ),
  );
}
```

### 步骤 2：验证功能

1. 滚动条是否正常显示
2. 拖动滚动条是否可以快速滚动
3. 原有的自动滚动功能是否正常
4. 重排模式下的滚动是否正常

## 影响范围

| 文件 | 方法 | 变更类型 | 风险等级 |
|------|------|----------|----------|
| `chapter_list_screen.dart` | `_buildNormalChapterList()` | 包裹 Scrollbar | 低 |

## 遵循原则

- KISS：使用最简单的原生方案
- YAGNI：仅实现所需功能
- DRY：复用现有 `_scrollController`
