# 移除书架页面封面图

## 任务上下文
- **任务**: 移除书架页面书本封面图，节省空间
- **用户需求**: 封面图占用空间且无实际用途
- **选定方案**: 完全移除封面图，使用简洁列表布局，预加载状态改为标题旁小图标
- **创建时间**: 2026-01-26 07:57:29

## 执行计划

### 步骤1: 移除封面图容器（leading部分）
- 删除整个 `leading: Stack(...)` 部分（第341-386行）
- 移除相关的高度、宽度、装饰等样式代码

### 步骤2: 在标题旁添加预加载状态图标
- 修改 `title` 字段，在小说标题前添加条件渲染的图标
- 当 `_isPreloading(novel.url)` 为真时，显示加载图标
- 使用 `Row` 包裹图标和文本

### 步骤3: 验证布局效果
- 检查 `contentPadding` 设置
- 确认列表项垂直间距合理

### 步骤4: 移除不再使用的代码（可选清理）
- 检查 `novel.coverUrl` 是否在其他地方使用

## 预期效果
- 列表项高度减少约60-70%
- 视觉更简洁，信息密度更高
- 预加载状态仍可清晰显示
