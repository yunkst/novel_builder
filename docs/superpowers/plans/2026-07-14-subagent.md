# Subagent 子 Agent 功能 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让主 Agent 通过 `dispatch_subagent` 工具派出子 Agent 独立执行子任务（如梳理大型小说大纲），用户可点击主气泡里的子任务卡片跳转查看子 Agent 的完整工作流。

**架构：** 子 Agent 复用现有 `AgentLoop` + `AgentScenario` 抽象，但**不走 `ScenarioSession`、不持久化**——由新增的 `SubagentRunner` 直接运行 `AgentLoop` 并用 `AgentChatState` 持有内存状态供 UI 渲染。事件隔离通过 emit 包装层给子 Agent 事件打 `runId` 标签实现（不侵入 `AgentEvent` 基类），主 `ScenarioSession` 按 runId 过滤掉子 Agent 事件。同轮多个 `dispatch_subagent` 用 `Future.wait` 并行，并发上限 4、排队上限 30。

**技术栈：** Flutter 3 / Dart 3、Riverpod 2、sqflite。复用 `lib/services/novel_agent/` 现有 ReAct 循环。

---

## 实现细化说明（对 spec 的补充，非偏离）

spec §10.1 写「`AgentEvent` 基类加 `runId` 字段」。实现时发现 `agent_event.dart` 所有子类用**位置参数 + const 构造**，且 `agent_loop.dart` 有大量 `emit(const AgentDoneEvent())` 调用。侵入式改基类会牵动十几个调用点。

**改用更轻的「emit 包装层」方案**：`AgentEvent` 基类加一个**可选命名参数** `runId`（不破坏现有位置参数与 const 调用），`SubagentRunner` 在调起子 `AgentLoop` 时传入一个**包装过的 emit 回调**，该回调把裸事件转换成带 `runId` 的事件再转发到全局流。`AgentLoop` 内部代码零改动——它继续 emit 裸事件，runId 由外层注入。

这样：
- `AgentEvent` 基类只加 `final String? runId;` 和命名构造参数（默认 null），所有现有 `const TextDeltaEvent('x')` 等调用**无需修改**。
- `SubagentRunner` 持有 runId，包装 emit。
- 主 `ScenarioSession` 监听全局流时，`event.runId == null` 的是主 Agent 事件（兼容旧路径），`event.runId != null` 的是子 Agent 事件——按 runId 路由。

---

## 文件结构

### 新增文件

| 文件 | 职责 | 依赖 |
|---|---|---|
| `lib/services/novel_agent/subagent_run.dart` | `SubagentRun` 内存模型 + `SubagentRunState` 枚举 + `SubagentProgress` 摘要投影 | `agent_chat_state.dart`、`cancellation_token.dart` |
| `lib/services/novel_agent/subagent_registry.dart` | `SubagentRegistry`：按 `parentSessionId` 索引的 run 注册表 | `subagent_run.dart` |
| `lib/services/novel_agent/subagent_scenario.dart` | `SubagentScenario`：`AgentScenario` 子类，工具白名单过滤 + 子 Agent system prompt | `agent_scenario.dart`、`agent_tools.dart` |
| `lib/services/novel_agent/subagent_runner.dart` | `SubagentRunner`：并发调度（4/30）+ 起子 AgentLoop + 事件 runId 包装 + `AgentChatState` 聚合 | 上述全部 + `novel_agent_service.dart`、`agent_loop.dart` |
| `lib/core/providers/subagent_providers.dart` | `subagentRegistryProvider`、`subagentRunProvider`、`subagentRunnerProvider` | Riverpod |
| `lib/screens/subagent_detail_screen.dart` | 子 Agent 详情页（只读 Agent Chat 视图 + 停止按钮） | `agent_chat` 组件 |
| `lib/widgets/agent_chat/subagent_tool_card.dart` | 主气泡里的子任务卡片（含进度摘要） | `subagent_run.dart` |
| 对应 7 个测试文件 | 见各任务 | — |

### 修改文件

| 文件 | 修改 |
|---|---|
| `lib/services/novel_agent/agent_event.dart` | 基类加可选 `runId` 字段 + 命名构造参数；子类加可选 `runId` 转发 |
| `lib/services/novel_agent/agent_tools.dart` | 加 `_dispatchSubagent` 工具 schema + 加入 `allTools`；加 `filterTools(allowed)` 静态方法 |
| `lib/services/novel_agent/agent_loop.dart` | 工具执行段拆「普通工具串行 + dispatch_subagent 并行」两组 |
| `lib/services/novel_agent/novel_agent_service.dart` | 运行态 key 改 `runId`；`sendMessage`/`resumeFromMessages` 接受可选 `runId` |
| `lib/core/providers/scenario_session.dart` | `_handleAgentEvent` 按 runId 过滤；cancel 时级联取消子 run |
| `lib/widgets/agent_chat/agent_message_bubble.dart` | 识别 `dispatch_subagent` ToolCallSegment → 渲染 `SubagentToolCard` |

---

## 任务 1：`AgentEvent` 加 `runId` 字段（非破坏性）

**文件：**
- 修改：`lib/services/novel_agent/agent_event.dart`
- 测试：`test/unit/services/novel_agent/agent_event_runid_test.dart`

**目的：** 给事件打标签的能力，是后续子 Agent 事件隔离的基础。保持完全向后兼容——现有 `const` 调用不动。

- [ ] **步骤 1：编写失败的测试**

创建 `test/unit/services/novel_agent/agent_event_runid_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:novel_app/services/novel_agent/agent_event.dart';

void main() {
  group('AgentEvent runId', () {
    test('基类默认 runId 为 null（兼容旧路径）', () {
      const event = TextDeltaEvent('hello');
      expect(event.runId, isNull);
    });

    test('可通过命名参数注入 runId', () {
      const event = TextDeltaEvent('hello', runId: 'sub-abc');
      expect(event.runId, 'sub-abc');
      expect(event.text, 'hello');
    });

    test('ToolCallEndEvent 带 runId 仍可构造', () {
      const event = ToolCallEndEvent('get_outline', 'tc1', 'result',
          runId: 'sub-abc');
      expect(event.runId, 'sub-abc');
      expect(event.success, isTrue);
    });

    test('AgentDoneEvent 默认无参构造仍可用（const 调用兼容）', () {
      const event = AgentDoneEvent();
      expect(event.runId, isNull);
    });

    test('CompactionEvent 带 runId 可构造', () {
      const event = CompactionEvent(
        removedChars: 100,
        originalChars: 200,
        keptMessageCount: 2,
        droppedMessageCount: 1,
        droppedAgentFromIndex: 0,
        runId: 'sub-abc',
      );
      expect(event.runId, 'sub-abc');
    });
  });
}
```

- [ ] **步骤 2：运行测试验证失败**

运行：`flutter test test/unit/services/novel_agent/agent_event_runid_test.dart`
预期：FAIL，编译错误 `'runId' isn't a parameter` 或类似。

- [ ] **步骤 3：实现 `runId` 字段**

修改 `agent_event.dart`：

基类加字段 + 命名构造参数：

```dart
sealed class AgentEvent {
  /// 运行实例标识。null = 主 Agent 旧路径；非 null = 某次 dispatch_subagent 派出的子 Agent。
  /// 由 NovelAgentService/SubagentRunner 在 emit 包装时注入。
  /// ⚠️ 未来新增 AgentEvent 子类时，必须同步加可选命名参数 `runId` 并转发给 super，
  /// 否则该事件无法被 EventTagger 打标、会导致事件隔离失效。
  final String? runId;
  const AgentEvent({this.runId});
}
```

每个子类加可选 `runId` 命名参数并转发给 super。示例（其余同理）：

```dart
class TextDeltaEvent extends AgentEvent {
  final String text;
  const TextDeltaEvent(this.text, {String? runId}) : super(runId: runId);
}

class ToolCallStartEvent extends AgentEvent {
  final String name;
  final Map<String, dynamic> args;
  final String toolCallId;
  const ToolCallStartEvent(this.name, this.args, this.toolCallId,
      {String? runId})
      : super(runId: runId);
}

class ToolCallEndEvent extends AgentEvent {
  final String name;
  final String toolCallId;
  final String result;
  final String? fullResult;
  final bool success;
  const ToolCallEndEvent(
    this.name,
    this.toolCallId,
    this.result, {
    this.fullResult,
    this.success = true,
    String? runId,
  }) : super(runId: runId);
}

class ToolProgressEvent extends AgentEvent {
  final String toolCallId;
  final int generatedChars;
  const ToolProgressEvent(this.toolCallId, this.generatedChars,
      {String? runId})
      : super(runId: runId);
}

class AgentDoneEvent extends AgentEvent {
  const AgentDoneEvent({String? runId}) : super(runId: runId);
}

class AgentErrorEvent extends AgentEvent {
  final String error;
  const AgentErrorEvent(this.error, {String? runId}) : super(runId: runId);
}

class CompactionEvent extends AgentEvent {
  // ... 保留所有现有字段 ...
  const CompactionEvent({
    required this.removedChars,
    required this.originalChars,
    required this.keptMessageCount,
    required this.droppedMessageCount,
    required this.droppedAgentFromIndex,
    String? runId,
  }) : super(runId: runId);
  // ... 保留 compressionRatio / description getter ...
}
```

> 关键：`AgentDoneEvent` 原来是 `const AgentDoneEvent()`，改成 `const AgentDoneEvent({String? runId})` 后，现有 `emit(const AgentDoneEvent())` 调用**仍然合法**（命名参数有默认值）。`CompactionEvent` 原来是位置 + required 命名参数，加一个可选命名参数 `runId` 也不破坏现有调用。

- [ ] **步骤 4：运行测试验证通过**

运行：`flutter test test/unit/services/novel_agent/agent_event_runid_test.dart`
预期：PASS（全部 5 个用例）

- [ ] **步骤 5：运行全量回归，确认未破坏现有事件消费者**

运行：`flutter test test/unit/services/novel_agent/`
预期：全部 PASS（`runId` 默认 null，旧消费者不受影响）

- [ ] **步骤 6：Commit**

```bash
git add novel_app/lib/services/novel_agent/agent_event.dart \
        novel_app/test/unit/services/novel_agent/agent_event_runid_test.dart
git commit -m "feat(novel_agent): AgentEvent 加可选 runId 字段（非破坏性，为子 Agent 事件隔离做准备）"
```

---

## 任务 2：`SubagentRun` 内存模型

**文件：**
- 创建：`lib/services/novel_agent/subagent_run.dart`
- 测试：`test/unit/services/novel_agent/subagent_run_test.dart`

**目的：** 子 Agent 单个运行实例的状态模型。不持久化。

- [ ] **步骤 1：编写失败的测试**

创建 `test/unit/services/novel_agent/subagent_run_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:novel_app/services/novel_agent/subagent_run.dart';

void main() {
  group('SubagentRun', () {
    test('初始状态为 pending', () {
      final run = SubagentRun(
        runId: 'r1',
        parentSessionId: 's1',
        task: '梳理前 30 章人物关系',
        allowedTools: const ['get_outline', 'read_chapter'],
        toolCallId: 'tc1',
      );
      expect(run.state, SubagentRunState.pending);
      expect(run.finalSummary, isNull);
      expect(run.errorMessage, isNull);
      expect(run.isTerminal, isFalse);
    });

    test('completed/failed/cancelled 为终态', () {
      for (final s in [
        SubagentRunState.completed,
        SubagentRunState.failed,
        SubagentRunState.cancelled,
      ]) {
        final run = SubagentRun(
          runId: 'r', parentSessionId: 's', task: 't', allowedTools: const [], toolCallId: 'tc');
        run.state = s;
        expect(run.isTerminal, isTrue, reason: '$s 应为终态');
      }
    });

    test('progressSummary 运行中取最后思考片段', () {
      final run = SubagentRun(
        runId: 'r', parentSessionId: 's', task: 't', allowedTools: const []);
      run.state = SubagentRunState.running;
      run.lastThought = '正在分析第 12 章的对话';
      run.lastToolName = 'read_chapter';
      final s = run.progressSummary;
      expect(s.contains('分析第 12 章'), isTrue);
      expect(s.contains('read_chapter'), isTrue);
    });

    test('progressSummary 完成取 finalSummary 首段', () {
      final run = SubagentRun(
        runId: 'r', parentSessionId: 's', task: 't', allowedTools: const []);
      run.state = SubagentRunState.completed;
      run.finalSummary = '## 最终结论\n发现 5 个主要人物。';
      expect(run.progressSummary.contains('最终结论'), isTrue);
    });

    test('progressSummary 失败取 errorMessage', () {
      final run = SubagentRun(
        runId: 'r', parentSessionId: 's', task: 't', allowedTools: const []);
      run.state = SubagentRunState.failed;
      run.errorMessage = 'LLM 配置缺失';
      expect(run.progressSummary.contains('LLM 配置缺失'), isTrue);
    });
  });
}
```

- [ ] **步骤 2：运行测试验证失败**

运行：`flutter test test/unit/services/novel_agent/subagent_run_test.dart`
预期：FAIL，`SubagentRun` / `SubagentRunState` 未定义。

- [ ] **步骤 3：实现 `SubagentRun`**

创建 `lib/services/novel_agent/subagent_run.dart`：

```dart
/// 子 Agent 单次运行的内存模型（不持久化）
///
/// 由 [SubagentRunner] 创建并持有，[SubagentRegistry] 按 parentSessionId 索引。
/// 一个 [SubagentRun] = 主 Agent 一次 dispatch_subagent 调用派出的子任务。
library;

import 'dart:async';

import '../../core/providers/agent_chat_state.dart';
import '../../utils/cancellation_token.dart';
import 'agent_event.dart';

/// 子 Agent 运行状态
enum SubagentRunState {
  /// 排队中（未达并发槽位）
  pending,
  /// 正在运行
  running,
  /// 正常完成
  completed,
  /// 异常终止
  failed,
  /// 被用户/主 Agent 取消
  cancelled,
}

class SubagentRun {
  final String runId;
  final String parentSessionId;
  final String task;
  final List<String> allowedTools;
  final DateTime createdAt;

  /// 主 Agent 调用 dispatch_subagent 的 toolCallId（LLM 生成）。
  /// 用于 UI 把主气泡里的 ToolCallSegment 反查到本 run（详见任务 10）。
  final String toolCallId;

  /// 可变状态：由 SubagentRunner 推进
  SubagentRunState state;

  /// 子 Agent 的 UI 状态（供详情页渲染 messages / streamingSegments）
  AgentChatState chatState;

  /// 取消令牌源；SubagentRunner 启动时创建，cancel() 时触发
  CancellationTokenSource? tokenSource;

  /// 全局事件流订阅（按 runId 过滤后更新 chatState）
  StreamSubscription<AgentEvent>? eventSub;

  /// 运行中实时跟踪（供主气泡卡片进度摘要用）
  String? lastThought;
  String? lastToolName;

  /// 终态产物
  String? finalSummary;
  String? errorMessage;

  SubagentRun({
    required this.runId,
    required this.parentSessionId,
    required this.task,
    required this.allowedTools,
    required this.toolCallId,
    DateTime? createdAt,
  })  : createdAt = createdAt ?? DateTime.now(),
        state = SubagentRunState.pending,
        chatState = const AgentChatState();

  /// 是否处于终态
  bool get isTerminal =>
      state == SubagentRunState.completed ||
      state == SubagentRunState.failed ||
      state == SubagentRunState.cancelled;

  /// 主气泡卡片用的轻量进度摘要（按 spec §8.3 投影规则）
  String get progressSummary {
    switch (state) {
      case SubagentRunState.pending:
        return '排队中…';
      case SubagentRunState.running:
        final parts = <String>[];
        if (lastToolName != null) parts.add('[$lastToolName]');
        if (lastThought != null) {
          // 取最近 40 字
          final t = lastThought!;
          parts.add(t.length > 40 ? '${t.substring(t.length - 40)}' : t);
        }
        return parts.isEmpty ? '运行中…' : parts.join(' ');
      case SubagentRunState.completed:
        if (finalSummary == null || finalSummary!.isEmpty) return '已完成';
        final firstLine = finalSummary!.split('\n').first;
        return firstLine.length > 60
            ? '${firstLine.substring(0, 60)}…'
            : firstLine;
      case SubagentRunState.failed:
        return errorMessage ?? '失败';
      case SubagentRunState.cancelled:
        return '已取消';
    }
  }
}
```

> 注：`CancellationTokenSource` 是否已存在需在步骤 3 实现前确认 `lib/utils/cancellation_token.dart`。若现有只有 `CancellationToken`（无 Source），任务 2.5 会先补一个最小的 Source。先看现有实现。

- [ ] **步骤 3a：确认 `CancellationToken` 现有 API**

运行：查看 `novel_app/lib/utils/cancellation_token.dart` 内容（Read 工具）。

若已含 `CancellationTokenSource`（含 `cancel()` + `token` getter），跳过步骤 3b。
若只有 `CancellationToken`（手动构造 + `cancel(reason)`），执行步骤 3b 补一个 Source。

- [ ] **步骤 3b（条件执行）：补 `CancellationTokenSource`**

仅在 3a 确认缺失时执行。在 `cancellation_token.dart` 末尾追加：

```dart
/// CancellationToken 的可取消句柄。
///
/// 主 Agent / SubagentRunner 持有 source，调 cancel() 触发；
/// AgentLoop 持有 source.token 做检查点判断。
class CancellationTokenSource {
  CancellationTokenSource() : token = CancellationToken();
  final CancellationToken token;
  bool _cancelled = false;
  bool get isCancelled => _cancelled;
  void cancel({String? reason}) {
    _cancelled = true;
    token.cancel(reason: reason ?? 'CancellationTokenSource.cancel');
  }
}
```

> 若现有 `CancellationToken` 构造/取消语义不同，按现有 API 适配；目标只是「一个持有方 cancel → AgentLoop 检查点感知」。

- [ ] **步骤 4：运行测试验证通过**

运行：`flutter test test/unit/services/novel_agent/subagent_run_test.dart`
预期：PASS（全部 5 个用例）

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/services/novel_agent/subagent_run.dart \
        novel_app/lib/utils/cancellation_token.dart \
        novel_app/test/unit/services/novel_agent/subagent_run_test.dart
git commit -m "feat(novel_agent): 新增 SubagentRun 内存模型（状态枚举 + 进度摘要投影）"
```

---

## 任务 3：`SubagentRegistry`

**文件：**
- 创建：`lib/services/novel_agent/subagent_registry.dart`
- 测试：`test/unit/services/novel_agent/subagent_registry_test.dart`

- [ ] **步骤 1：编写失败的测试**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:novel_app/services/novel_agent/subagent_registry.dart';
import 'package:novel_app/services/novel_agent/subagent_run.dart';

void main() {
  group('SubagentRegistry', () {
    test('create 后可按 (session, runId) 查到', () {
      final reg = SubagentRegistry();
      final run = reg.create(
        parentSessionId: 's1',
        task: 't',
        allowedTools: const ['get_outline'],
      );
      expect(run.runId, isNotEmpty);
      expect(reg.get('s1', run.runId), same(run));
    });

    test('create 传 toolCallId 后可按 toolCallId 反查', () {
      final reg = SubagentRegistry();
      final run = reg.create(
        parentSessionId: 's1',
        task: 't',
        allowedTools: const ['get_outline'],
        toolCallId: 'tc42',
      );
      expect(reg.getByToolCallId('s1', 'tc42'), same(run));
      expect(reg.getByToolCallId('s1', 'other'), isNull);
      expect(reg.getByToolCallId('s2', 'tc42'), isNull); // 跨 session 不可见
    });

    test('不同 session 的 run 互不可见', () {
      final reg = SubagentRegistry();
      final r1 = reg.create(parentSessionId: 's1', task: 't', allowedTools: const []);
      reg.create(parentSessionId: 's2', task: 't', allowedTools: const []);
      expect(reg.get('s2', r1.runId), isNull);
    });

    test('listForSession 只返回该 session 的 run', () {
      final reg = SubagentRegistry();
      reg.create(parentSessionId: 's1', task: 'a', allowedTools: const []);
      reg.create(parentSessionId: 's1', task: 'b', allowedTools: const []);
      reg.create(parentSessionId: 's2', task: 'c', allowedTools: const []);
      expect(reg.listForSession('s1').length, 2);
      expect(reg.listForSession('s2').length, 1);
    });

    test('countActiveBySession 统计 running+pending', () {
      final reg = SubagentRegistry();
      final r1 = reg.create(parentSessionId: 's1', task: 't', allowedTools: const []);
      r1.state = SubagentRunState.running;
      final r2 = reg.create(parentSessionId: 's1', task: 't', allowedTools: const []);
      r2.state = SubagentRunState.pending; // 排队
      reg.create(parentSessionId: 's1', task: 't', allowedTools: const [])
        ..state = SubagentRunState.completed; // 终态不计
      expect(reg.countActiveBySession('s1'), 2);
    });

    test('remove 删除指定 run', () {
      final reg = SubagentRegistry();
      final r = reg.create(parentSessionId: 's1', task: 't', allowedTools: const []);
      reg.remove('s1', r.runId);
      expect(reg.get('s1', r.runId), isNull);
    });

    test('clearForSession 清空该 session 全部 run', () {
      final reg = SubagentRegistry();
      reg.create(parentSessionId: 's1', task: 't', allowedTools: const []);
      reg.create(parentSessionId: 's1', task: 't', allowedTools: const []);
      reg.clearForSession('s1');
      expect(reg.listForSession('s1'), isEmpty);
    });

    test('pruneForSession 保留最近 N 个终态 run，清掉更早的', () {
      final reg = SubagentRegistry();
      for (var i = 0; i < 25; i++) {
        final r = reg.create(parentSessionId: 's1', task: 't$i', allowedTools: const []);
        r.state = SubagentRunState.completed;
      }
      reg.pruneForSession('s1', keep: 20);
      expect(reg.listForSession('s1').length, 20);
    });
  });
}
```

- [ ] **步骤 2：运行测试验证失败**

运行：`flutter test test/unit/services/novel_agent/subagent_registry_test.dart`
预期：FAIL，`SubagentRegistry` 未定义。

- [ ] **步骤 3：实现 `SubagentRegistry`**

创建 `lib/services/novel_agent/subagent_registry.dart`：

```dart
import 'subagent_run.dart';

/// 子 Agent 注册表（内存，按 parentSessionId 索引）
///
/// 生命周期：随 ScenarioSessionsNotifier 一起 dispose（clearForSession）。
/// 不持久化。
library;

class SubagentRegistry {
  final Map<String, Map<String, SubagentRun>> _runsBySession = {};
  final Map<String, Map<String, SubagentRun>> _toolCallIndex = {};

  int _seq = 0;

  /// 生成 runId（不用 uuid 避免引入依赖；session 内唯一即可）
  String _newRunId(String sessionId) {
    _seq++;
    return 'sub-${sessionId.hashCode.toRadixString(36)}-$_seq';
  }

  SubagentRun create({
    required String parentSessionId,
    required String task,
    required List<String> allowedTools,
    String toolCallId = '', // 由 SubagentRunner.dispatch 传入父 toolCallId；测试可省略
  }) {
    final run = SubagentRun(
      runId: _newRunId(parentSessionId),
      parentSessionId: parentSessionId,
      task: task,
      allowedTools: List<String>.unmodifiable(allowedTools),
      toolCallId: toolCallId,
    );
    // 同时索引 toolCallId → run（同一 session 内 toolCallId 唯一）
    if (toolCallId.isNotEmpty) {
      (_toolCallIndex[parentSessionId] ??= <String, SubagentRun>{})[toolCallId] = run;
    }
    (_runsBySession[parentSessionId] ??= <String, SubagentRun>{})[run.runId] = run;
    return run;
  }

  SubagentRun? get(String parentSessionId, String runId) =>
      _runsBySession[parentSessionId]?[runId];

  /// 按 toolCallId 反查（供 UI 从主气泡 ToolCallSegment 找到子 run）
  SubagentRun? getByToolCallId(String parentSessionId, String toolCallId) =>
      _toolCallIndex[parentSessionId]?[toolCallId];

  List<SubagentRun> listForSession(String parentSessionId) {
    final m = _runsBySession[parentSessionId];
    if (m == null) return const <SubagentRun>[];
    // 按 createdAt 升序，便于 UI 稳定展示
    final list = m.values.toList()
      ..sort((a, b) => a.createdAt.compareTo(b.createdAt));
    return list;
  }

  /// 统计某 session 正在占用槽位的 run（running + pending）
  int countActiveBySession(String parentSessionId) {
    final m = _runsBySession[parentSessionId];
    if (m == null) return 0;
    return m.values.where((r) =>
        r.state == SubagentRunState.running ||
        r.state == SubagentRunState.pending).length;
  }

  /// 统计某 session 全部活跃 run（含排队），用于 30 上限判断
  int countTotalBySession(String parentSessionId) {
    final m = _runsBySession[parentSessionId];
    if (m == null) return 0;
    return m.values.where((r) => !r.isTerminal).length;
  }

  void remove(String parentSessionId, String runId) {
    final run = _runsBySession[parentSessionId]?.remove(runId);
    if (run != null && run.toolCallId.isNotEmpty) {
      _toolCallIndex[parentSessionId]?.remove(run.toolCallId);
    }
  }

  void clearForSession(String parentSessionId) {
    _runsBySession.remove(parentSessionId);
  }

  /// 保留最近 keep 个 run（不限终态），清掉更早的——控制内存
  /// 用于「保留最近 N 个供回看」（spec §5.3 N=20）
  void pruneForSession(String parentSessionId, {required int keep}) {
    final m = _runsBySession[parentSessionId];
    if (m == null) return;
    if (m.length <= keep) return;
    final sorted = m.values.toList()
      ..sort((a, b) => b.createdAt.compareTo(a.createdAt)); // 新→旧
    final toRemove = sorted.skip(keep);
    for (final r in toRemove) {
      m.remove(r.runId);
    }
  }
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`flutter test test/unit/services/novel_agent/subagent_registry_test.dart`
预期：PASS（全部 7 个用例）

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/services/novel_agent/subagent_registry.dart \
        novel_app/test/unit/services/novel_agent/subagent_registry_test.dart
git commit -m "feat(novel_agent): 新增 SubagentRegistry（按 session 索引的内存注册表）"
```

---

## 任务 4：`AgentTools` 加 `dispatch_subagent` schema + `filterTools`

**文件：**
- 修改：`lib/services/novel_agent/agent_tools.dart`
- 测试：`test/unit/services/novel_agent/agent_tools_subagent_test.dart`

- [ ] **步骤 1：编写失败的测试**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:novel_app/services/novel_agent/agent_tools.dart';

void main() {
  group('AgentTools subagent', () {
    test('allTools 含 dispatch_subagent', () {
      final t = AgentTools.findTool('dispatch_subagent');
      expect(t, isNotNull);
      expect(t!['function']['parameters']['properties'] containsKey('task'), isTrue);
      expect(t['function']['parameters']['properties'] containsKey('allowed_tools'), isTrue);
      expect(
        (t['function']['parameters']['required'] as List).contains('task'),
        isTrue,
      );
    });

    test('filterTools 返回白名单内工具且不含 dispatch_subagent', () {
      final filtered = AgentTools.filterTools(
        const ['get_outline', 'read_chapter', 'list_chapters'],
      );
      final names = filtered.map((t) => t['function']['name'] as String).toSet();
      expect(names.contains('get_outline'), isTrue);
      expect(names.contains('read_chapter'), isTrue);
      expect(names.contains('list_chapters'), isTrue);
      expect(names.contains('dispatch_subagent'), isFalse); // 子 Agent 不能再派
      expect(names.contains('update_chapter_content'), isFalse); // 白名单外
    });

    test('filterTools 自动剔除 dispatch_subagent（即使传入）', () {
      final filtered = AgentTools.filterTools(
        const ['get_outline', 'dispatch_subagent'],
      );
      final names = filtered.map((t) => t['function']['name'] as String).toSet();
      expect(names.contains('dispatch_subagent'), isFalse);
      expect(names.contains('get_outline'), isTrue);
    });

    test('filterTools 白名单全空返回空列表（不崩）', () {
      expect(AgentTools.filterTools(const []), isEmpty);
    });

    test('filterTools 忽略不存在的工具名', () {
      final filtered = AgentTools.filterTools(
        const ['get_outline', 'nonexistent_tool'],
      );
      expect(filtered.length, 1);
      expect(filtered.first['function']['name'], 'get_outline');
    });
  });
}
```

- [ ] **步骤 2：运行测试验证失败**

运行：`flutter test test/unit/services/novel_agent/agent_tools_subagent_test.dart`
预期：FAIL，`findTool('dispatch_subagent')` 返回 null，`filterTools` 未定义。

- [ ] **步骤 3：实现**

在 `agent_tools.dart` 修改：

（a）在 `allTools` 列表末尾加入 `_dispatchSubagent`：

```dart
  static const List<Map<String, dynamic>> allTools = [
    // ... 现有工具 ...
    _createImageToVideo,
    // ===== 子 Agent =====
    _dispatchSubagent,
  ];
```

（b）在类内加工具定义 + `filterTools` 静态方法：

```dart
  static const _dispatchSubagent = {
    'type': 'function',
    'function': {
      'name': 'dispatch_subagent',
      'description':
          '派出一个子 Agent 独立执行子任务，适用于可拆分的复杂工作（如梳理大量章节的人物关系、'
          '分卷总结大纲）。子 Agent 只能使用 allowed_tools 列表中的工具，无法继续派子 Agent。\n'
          '使用要点：\n'
          '- task 要足够具体，让子 Agent 能独立完成\n'
          '- allowed_tools 只放子 Agent 真正需要的工具\n'
          '- 同一轮可派多个 dispatch_subagent 并行执行\n'
          '- 子 Agent 完成后返回结构化 Markdown 总结',
      'parameters': {
        'type': 'object',
        'properties': {
          'task': {
            'type': 'string',
            'description':
                "交给子 Agent 的任务说明，例如：'请阅读第1-30章并梳理主要人物关系，'
                '输出每个人物的出场章节和关系。'",
          },
          'allowed_tools': {
            'type': 'array',
            'items': {'type': 'string'},
            'description':
                '子 Agent 可调用的工具名白名单。必须从当前可用工具中选择，'
                '不能包含 dispatch_subagent（子 Agent 不能再派子 Agent）。',
          },
        },
        'required': ['task', 'allowed_tools'],
      },
    },
  };

  /// 按白名单过滤工具定义（供 SubagentScenario 使用）
  ///
  /// - 自动剔除 `dispatch_subagent`（强制单层嵌套）
  /// - 忽略不存在的工具名
  static List<Map<String, dynamic>> filterTools(List<String> allowed) {
    final allowedSet = allowed.toSet()..remove('dispatch_subagent');
    if (allowedSet.isEmpty) return const <Map<String, dynamic>>[];
    return allTools
        .where((t) => allowedSet.contains(t['function']['name']))
        .toList(growable: false);
  }
```

- [ ] **步骤 4：运行测试验证通过**

运行：`flutter test test/unit/services/novel_agent/agent_tools_subagent_test.dart`
预期：PASS（全部 5 个用例）

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/services/novel_agent/agent_tools.dart \
        novel_app/test/unit/services/novel_agent/agent_tools_subagent_test.dart
git commit -m "feat(novel_agent): AgentTools 注册 dispatch_subagent + filterTools 白名单过滤"
```

---

## 任务 5：`SubagentScenario`（子 Agent 场景）

**文件：**
- 创建：`lib/services/novel_agent/subagent_scenario.dart`
- 测试：`test/unit/services/novel_agent/subagent_scenario_test.dart`

**目的：** `AgentScenario` 子类。负责工具白名单过滤 + 子 Agent system prompt。**不实现 `dispatch_subagent` 本身**（那个在 AgentLoop 改造里拦截），只确保子 Agent 看不到也不能调它。

- [ ] **步骤 1：编写失败的测试**

需要 mock 的最小 `Ref`：现有测试若已有 helper（搜 `test/mocks/` 或 `test/helpers/`）则复用；否则本测试只覆盖不需要 Ref 的纯逻辑（tools 过滤、prompt 构建、executeTool 白名单拒绝）。

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:novel_app/services/novel_agent/subagent_scenario.dart';
import 'package:novel_app/services/novel_agent/agent_scenario.dart';

void main() {
  group('SubagentScenario', () {
    SubagentScenario make(List<String> allowed) =>
        SubagentScenario(task: 't', allowedTools: allowed);

    test('tools 只含白名单内工具', () {
      final s = make(['get_outline', 'read_chapter']);
      final names = s.tools.map((t) => t['function']['name'] as String).toSet();
      expect(names, {'get_outline', 'read_chapter'});
    });

    test('tools 不含 dispatch_subagent（即使白名单传入）', () {
      final s = make(['get_outline', 'dispatch_subagent']);
      final names = s.tools.map((t) => t['function']['name'] as String).toSet();
      expect(names.contains('dispatch_subagent'), isFalse);
    });

    test('id 为 subagent', () {
      expect(make(['get_outline']).id, 'subagent');
    });

    test('buildSystemPrompt 含通用模板纪律', () {
      final s = make(['get_outline']);
      final prompt = s.buildSystemPrompt(const AgentScenarioContext());
      expect(prompt.contains('子 Agent'), isTrue);
      expect(prompt.contains('最终结论'), isTrue); // 结果格式
      expect(prompt.contains('不要再派子 Agent'), isTrue); // 单层嵌套约束
    });

    test('buildSystemPrompt 含 task 文本', () {
      final s = SubagentScenario(
          task: '梳理第 1-30 章人物关系', allowedTools: ['get_outline']);
      final prompt = s.buildSystemPrompt(const AgentScenarioContext());
      expect(prompt.contains('梳理第 1-30 章人物关系'), isTrue);
    });

    test('executeTool 白名单外工具返回 guidanceError JSON', () async {
      final s = make(['get_outline']);
      final result = await s.executeTool('update_chapter_content', {});
      // result 是 JSON 字符串
      expect(result.contains('error'), isTrue);
      expect(result.contains('forbidden_tool') || result.contains('not_allowed'),
          isTrue);
    });

    test('executeTool 调用 dispatch_subagent 返回禁止错误', () async {
      final s = make(['get_outline']);
      final result = await s.executeTool('dispatch_subagent', {});
      expect(result.contains('error'), isTrue);
    });
  });
}
```

- [ ] **步骤 2：运行测试验证失败**

运行：`flutter test test/unit/services/novel_agent/subagent_scenario_test.dart`
预期：FAIL，`SubagentScenario` 未定义。

- [ ] **步骤 3：实现 `SubagentScenario`**

创建 `lib/services/novel_agent/subagent_scenario.dart`：

```dart
/// 子 Agent 场景：AgentScenario 子类
///
/// 与主场景（WritingScenario）的差异：
/// - tools 由 allowedTools 白名单过滤（强制不含 dispatch_subagent）
/// - system prompt 用通用子 Agent 模板（纪律 + 结果格式 + 单层嵌套约束）
/// - executeTool 调用前校验白名单，越权返回 guidanceError
/// - 不持久化、不挂经验记忆（getMemories 返回空）
library;

import 'dart:convert';

import 'agent_scenario.dart';
import 'agent_tools.dart';

class SubagentScenario with AgentScenarioCleanupMixin implements AgentScenario {
  final String task;
  final List<String> allowedTools;

  SubagentScenario({required this.task, required this.allowedTools});

  @override
  String get id => 'subagent';

  @override
  String get displayName => '子 Agent';

  @override
  List<Map<String, dynamic>> get tools => AgentTools.filterTools(allowedTools);

  @override
  String buildSystemPrompt(AgentScenarioContext context) {
    return '''你是一个专注的子 Agent。你的任务由父 Agent 明确指定，你只能使用被授权的工具列表。

## 你的任务
$task

## 纪律
1. 每次调用工具前，先用一行简洁说明你的思考。
2. 只能使用 allowed_tools 中的工具；越权调用会被拒绝。
3. 读-写分离：写入工具（如 update_outline、update_chapter_content）前必须先读取当前内容。
4. 完成或失败后必须停止，不要再派子 Agent（你也没有这个能力）。

## 结果格式
最终结果必须是结构化的 Markdown，包含：
- ## 任务目标
- ## 执行步骤
- ## 关键发现
- ## 最终结论
''';
  }

  @override
  Future<String> executeTool(
    String name,
    Map<String, dynamic> args, {
    void Function(int generatedChars)? onProgress,
  }) async {
    // 双重保险：dispatch_subagent 永远不在子 Agent 可调工具里
    if (name == 'dispatch_subagent') {
      return jsonEncode({
        'error': 'forbidden_tool',
        'message': '子 Agent 不能调用 dispatch_subagent。',
      });
    }
    // 白名单校验
    if (!allowedTools.contains(name)) {
      return jsonEncode({
        'error': 'forbidden_tool',
        'message': '工具 $name 不在你被授权的工具列表中。'
            '你只能使用：${allowedTools.join(", ")}',
      });
    }
    // 实际工具执行委托：子 Agent 场景不自己执行具体工具，
    // 由 SubagentRunner 注入一个 delegate（通过构造时 setCleanupTask 之外的渠道）。
    // 见任务 6 SubagentRunner 组装时注入 _toolExecutor。
    // 这里若 delegate 未注入，返回错误。
    final delegate = _toolDelegate;
    if (delegate == null) {
      return jsonEncode({
        'error': 'not_configured',
        'message': '子 Agent 工具执行未配置。',
      });
    }
    return delegate(name, args, onProgress);
  }

  /// 由 SubagentRunner 注入的实际工具执行委托。
  /// 签名与 AgentScenario.executeTool 一致。
  void Function(String, Map<String, dynamic>, void Function(int)?)? _toolDelegate;
  void setToolDelegate(
      Future<String> Function(String, Map<String, dynamic>, void Function(int)?)
          delegate) {
    _toolDelegate = delegate;
  }

  @override
  Future<List<String>> getMemories() async => const [];
}
```

> 设计说明：`SubagentScenario` 把「工具实际执行」委托给一个由 `SubagentRunner` 注入的 delegate（复用主 Agent 的 `ToolExecutor`），自己只负责白名单校验 + prompt。这样子 Agent 调 `get_outline` 等走的是和主 Agent 完全一致的执行路径（含 read-before-write 校验），只是被白名单约束。

- [ ] **步骤 4：运行测试验证通过**

运行：`flutter test test/unit/services/novel_agent/subagent_scenario_test.dart`
预期：PASS（全部 7 个用例）

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/services/novel_agent/subagent_scenario.dart \
        novel_app/test/unit/services/novel_agent/subagent_scenario_test.dart
git commit -m "feat(novel_agent): 新增 SubagentScenario（白名单过滤 + 通用 prompt + 工具委托）"
```

---

## 任务 6：`SubagentRunner`（并发调度 + 起子 AgentLoop + 事件聚合）

**文件：**
- 创建：`lib/services/novel_agent/subagent_runner.dart`
- 测试：`test/unit/services/novel_agent/subagent_runner_test.dart`

**目的：** 核心。负责 4/30 并发调度、起子 `AgentLoop`、emit 包装打 runId、聚合 `AgentChatState`、提取 finalSummary。这是最复杂的任务，拆分子步骤。

- [ ] **步骤 1：编写并发上限测试（失败）**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:novel_app/services/novel_agent/subagent_runner.dart';
import 'package:novel_app/services/novel_agent/subagent_registry.dart';

void main() {
  group('SubagentRunner 并发控制', () {
    test('单轮 30 个以内全部入队（不立即拒绝）', () async {
      final registry = SubagentRegistry();
      final runner = SubagentRunner.forTest(registry: registry);
      // 派 5 个，前 4 个 running，第 5 个 pending（用模拟 runner 不真起 loop）
      for (var i = 0; i < 5; i++) {
        await runner.enqueueForTest('s1', task: 't$i', allowedTools: const ['get_outline']);
      }
      expect(registry.countTotalBySession('s1'), 5);
      expect(registry.countActiveBySession('s1'), 5);
    });

    test('单轮第 31 个返回 max_subagents_reached 错误', () async {
      final registry = SubagentRegistry();
      final runner = SubagentRunner.forTest(registry: registry);
      for (var i = 0; i < 30; i++) {
        await runner.enqueueForTest('s1', task: 't$i', allowedTools: const ['get_outline']);
      }
      final result = await runner.enqueueForTest('s1', task: 't31', allowedTools: const ['get_outline']);
      // 返回的是工具结果 JSON 字符串
      expect(result.contains('max_subagents_reached'), isTrue);
    });
  });
}
```

- [ ] **步骤 2：运行测试验证失败**

运行：`flutter test test/unit/services/novel_agent/subagent_runner_test.dart`
预期：FAIL，`SubagentRunner` 未定义。

- [ ] **步骤 3：实现 `SubagentRunner` 骨架（并发控制 + 队列）**

创建 `lib/services/novel_agent/subagent_runner.dart`：

```dart
/// 子 Agent 运行调度器
///
/// 职责：
/// - 4 并发 + 30 排队上限（按 parentSessionId 隔离计数）
/// - 起子 AgentLoop，emit 包装打 runId
/// - 聚合子 Agent AgentChatState（供详情页）
/// - 提取 finalSummary 返回给主 Agent
///
/// 并发模型：同一 parentSessionId 内，running < 4 立即启动，
/// 4 ≤ total ≤ 29 入队 FIFO，total ≥ 30 立即返回 max_subagents_reached。
library;

import 'dart:async';
import 'dart:convert';

import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../logger_service.dart';
import '../dsl_engine/llm_provider.dart';
import '../../utils/cancellation_token.dart';
import 'agent_event.dart';
import 'agent_loop.dart';
import 'agent_scenario.dart';
import 'subagent_registry.dart';
import 'subagent_scenario.dart';
import 'subagent_run.dart';

class SubagentRunner {
  static const int maxConcurrent = 4;
  static const int maxQueue = 30;

  final Ref ref;
  final SubagentRegistry registry;

  SubagentRunner({required this.ref, required this.registry});

  /// 测试用工厂：允许注入 registry 且不依赖完整 Ref（forTest 路径不发真请求）
  factory SubagentRunner.forTest({required SubagentRegistry registry}) {
    // forTest 时 ref 用一个 throw-on-use 的占位；测试只覆盖并发计数与拒绝逻辑
    return SubagentRunner(ref: _ThrowRef(), registry: registry);
  }

  /// 入队一个子任务。超过上限立即返回错误 JSON；否则返回 run 后的结果 JSON。
  ///
  /// [emitParent] 主 Agent 的 emit 回调（用于把子 Agent 的 ToolCallStart/End
  /// 透传给主 session——见任务 7 AgentLoop 改造）。
  /// [parentCurrentNovelId] 父 session 当前小说 id，子 Agent 通过 WritingScenarioContext
  /// 继承，让 get_outline 等隐式工具作用于同一本书（null 表示子 Agent 自己 select_novel）。
  Future<String> dispatch({
    required String parentSessionId,
    required String task,
    required List<String> allowedTools,
    required String parentToolCallId,
    int? parentCurrentNovelId,
    void Function(AgentEvent)? emitParent,
  }) async {
    // 上限检查
    if (registry.countTotalBySession(parentSessionId) >= maxQueue) {
      LoggerService.instance.w(
        'dispatch_subagent 拒绝：已达单轮上限 $maxQueue (session=$parentSessionId)',
        category: LogCategory.ai,
        tags: ['agent', 'subagent', 'max_reached'],
      );
      return jsonEncode({
        'error': 'max_subagents_reached',
        'message': '当前已有 $maxQueue 个子 Agent 在跑或排队，请等其中一些完成后再派。',
      });
    }

    final run = registry.create(
      parentSessionId: parentSessionId,
      task: task,
      allowedTools: allowedTools,
      toolCallId: parentToolCallId, // 记录父 toolCallId，UI 可反查
    );

    // 校验 allowed_tools 合法性（含 dispatch_subagent 剔除已由 filterTools 兜底）
    if (task.trim().isEmpty) {
      run.state = SubagentRunState.failed;
      run.errorMessage = 'task 不能为空';
      return jsonEncode({'error': 'missing_param', 'message': 'task 不能为空'});
    }

    // 并发槽位控制：若 running 已满，挂起等待槽位释放
    await _waitForSlot(parentSessionId);

    await _runOne(run, emitParent: emitParent);

    // 返回结果 JSON 给主 Agent
    return _buildResultJson(run);
  }

  /// 等待直到该 session 有 running 空位
  Future<void> _waitForSlot(String sessionId) async {
    while (registry.countActiveBySession(sessionId) > maxConcurrent) {
      // pending 状态；等待 50ms 重检。真正并发场景下槽位由 _runOne 完成时释放。
      await Future<void>.delayed(const Duration(milliseconds: 50));
    }
  }

  /// 实际起一个子 AgentLoop
  Future<void> _runOne(SubagentRun run,
      {void Function(AgentEvent)? emitParent}) async {
    run.state = SubagentRunState.running;
    run.tokenSource = CancellationTokenSource();

    try {
      // 构造 SubagentScenario 并注入工具委托
      final scenario = SubagentScenario(
        task: run.task,
        allowedTools: run.allowedTools,
      );
      // 工具委托：复用主 Agent 的 ToolExecutor（通过 ref 构造）
      scenario.setToolDelegate((name, args, onProgress) async {
        // 见步骤 4：注入真实 ToolExecutor
        return _executeToolForSubagent(name, args, onProgress, run);
      });

      // 构造 LLM + Loop（复用 NovelAgentService 的 _buildAgentEnv 逻辑——
      // 步骤 4 会把它抽出为可复用方法）
      final llm = await _buildLlmForScenario('subagent');
      final loop = AgentLoop(llm: llm, scenario: scenario);

      // ★ emit 包装：给裸事件打 runId，并同步更新 run.chatState / run.lastThought
      await loop.run(
        initialMessages: [
          ChatMessage(role: 'user', content: run.task),
        ],
        systemPrompt: scenario.buildSystemPrompt(const AgentScenarioContext()),
        emit: (event) => _onSubagentEvent(event, run, emitParent),
        cancellationToken: run.tokenSource!.token,
      );

      // 提取 finalSummary：子 Agent 最后一条 assistant 文本
      run.finalSummary = _extractSummary(run.chatState);
      run.state = run.tokenSource!.isCancelled
          ? SubagentRunState.cancelled
          : SubagentRunState.completed;
    } catch (e, stack) {
      LoggerService.instance.e('子 Agent 运行失败 (runId=${run.runId}): $e',
          stackTrace: stack.toString(),
          category: LogCategory.ai,
          tags: ['agent', 'subagent', 'error']);
      run.state = SubagentRunState.failed;
      run.errorMessage = e.toString();
    } finally {
      await run.eventSub?.cancel();
      run.eventSub = null;
    }
  }

  void _onSubagentEvent(AgentEvent event, SubagentRun run,
      void Function(AgentEvent)? emitParent) {
    // 打 runId（复用裸事件 → 带 runId 事件）
    final tagged = _tagWithRunId(event, run.runId);
    // 更新 run 的进度跟踪 + chatState（复用 ScenarioSession 的投影逻辑——
    // 步骤 5 抽出 _SubagentStateProjector）
    _projectToChatState(tagged, run);
    // 转发到全局流（主 session 按 runId 过滤会忽略；详情页订阅会接收）
    emitParent?.call(tagged);
  }

  AgentEvent _tagWithRunId(AgentEvent event, String runId) {
    // 用 copyWith-style 重建带 runId 的事件。见步骤 5 实现。
    // 简化版：switch 各事件类型重建。
    return _EventTagger.tag(event, runId);
  }

  void _projectToChatState(AgentEvent event, SubagentRun run) {
    // 见步骤 5：抽出 _SubagentStateProjector，复用 agent_chat 的投影规则
    _SubagentStateProjector.project(event, run);
  }

  String _extractSummary(AgentChatState state) {
    // 取 messages 最后一条 assistant 消息的文本内容（AgentChatMessage.content getter）
    // 注：AgentChatMessage 的 content getter 把所有 TextSegment 拼接而成
    final msgs = state.messages;
    for (var i = msgs.length - 1; i >= 0; i--) {
      if (msgs[i].role == AgentChatRole.assistant) {
        final text = msgs[i].content; // getter，自动 join 所有 TextSegment
        if (text.trim().isNotEmpty) return text;
      }
    }
    return '';
  }

  String _buildResultJson(SubagentRun run) {
    switch (run.state) {
      case SubagentRunState.completed:
        return jsonEncode({
          'success': true,
          'summary': run.finalSummary ?? '',
          'runId': run.runId,
        });
      case SubagentRunState.cancelled:
        return jsonEncode({
          'error': 'cancelled',
          'message': '子 Agent 被取消。',
          'runId': run.runId,
        });
      case SubagentRunState.failed:
        return jsonEncode({
          'error': 'subagent_failed',
          'message': run.errorMessage ?? '子 Agent 运行失败',
          'runId': run.runId,
        });
      default:
        return jsonEncode({'error': 'unknown_state', 'runId': run.runId});
    }
  }

  // —— 步骤 4/5 填充的依赖方法 ——
  Future<LlmProvider> _buildLlmForScenario(String scenarioId) async {
    throw UnimplementedError('步骤 4 实现：复用 NovelAgentService._buildAgentEnv');
  }

  Future<String> _executeToolForSubagent(
      String name, Map<String, dynamic> args,
      void Function(int)? onProgress, SubagentRun run) async {
    throw UnimplementedError('步骤 4 实现：委托主 ToolExecutor');
  }

  /// 取消某 session 全部活跃 run（主 Agent cancel 时级联）
  void cancelAllForSession(String sessionId) {
    for (final run in registry.listForSession(sessionId)) {
      if (!run.isTerminal) {
        run.tokenSource?.cancel(reason: '主 Agent 取消');
      }
    }
  }
}

class _ThrowRef implements Ref {
  // forTest 占位；调用任何 read/watch 抛错（测试不发真请求）
  @override
  dynamic noSuchMethod(Invocation invocation) =>
      throw StateError('forTest Ref 不支持真实调用');
}
```

> 注：此骨架有 3 个 `UnimplementedError` 占位（`_buildLlmForScenario`、`_executeToolForSubagent`、`_EventTagger`/`_SubagentStateProjector`），由步骤 4、5 填充。`forTest` 路径只覆盖并发计数与拒绝逻辑，不触发这些方法。

- [ ] **步骤 3a：实现 `forTest` 路径的 `enqueueForTest`**

由于 `dispatch` 会调 `_waitForSlot` + `_runOne`（依赖未实现方法），测试用的并发计数需要一条**不入队执行**的路径。在 `SubagentRunner` 加测试专用方法：

```dart
  /// 测试专用：只入队 registry 不真跑（用于验证并发上限/拒绝逻辑）
  Future<String> enqueueForTest(
      String sessionId, {required String task, required List<String> allowedTools}) async {
    if (registry.countTotalBySession(sessionId) >= maxQueue) {
      return jsonEncode({
        'error': 'max_subagents_reached',
        'message': '已达上限',
      });
    }
    final run = registry.create(
        parentSessionId: sessionId, task: task, allowedTools: allowedTools);
    // 模拟 pending：不切 running，但计入 active
    return jsonEncode({'success': true, 'runId': run.runId});
  }
```

- [ ] **步骤 4：运行并发测试验证通过**

运行：`flutter test test/unit/services/novel_agent/subagent_runner_test.dart`
预期：PASS（2 个用例）

- [ ] **步骤 5：补充 runId 打标 + chatState 投影（`_EventTagger` + `_SubagentStateProjector`）**

在 `subagent_runner.dart` 内补：

```dart
class _EventTagger {
  static AgentEvent tag(AgentEvent e, String runId) {
    return switch (e) {
      TextDeltaEvent(:final text) => TextDeltaEvent(text, runId: runId),
      ToolCallStartEvent(:final name, :final args, :final toolCallId) =>
        ToolCallStartEvent(name, args, toolCallId, runId: runId),
      ToolCallEndEvent(:final name, :final toolCallId, :final result,
          :final fullResult, :final success) =>
        ToolCallEndEvent(name, toolCallId, result,
            fullResult: fullResult, success: success, runId: runId),
      ToolProgressEvent(:final toolCallId, :final generatedChars) =>
        ToolProgressEvent(toolCallId, generatedChars, runId: runId),
      AgentDoneEvent() => AgentDoneEvent(runId: runId),
      AgentErrorEvent(:final error) => AgentErrorEvent(error, runId: runId),
      CompactionEvent(:final removedChars, :final originalChars,
          :final keptMessageCount, :final droppedMessageCount,
          :final droppedAgentFromIndex) =>
        CompactionEvent(
          removedChars: removedChars,
          originalChars: originalChars,
          keptMessageCount: keptMessageCount,
          droppedMessageCount: droppedMessageCount,
          droppedAgentFromIndex: droppedAgentFromIndex,
          runId: runId,
        ),
    };
  }
}
```

`_SubagentStateProjector.project`：参考 `ScenarioSession._handleAgentEvent` 的投影逻辑。**实现者必读**：`scenario_session.dart` 中 `_handleAgentEvent` 与 `_finalizeAgentResponse` 高度耦合 session 内部状态（DB 持久化、currentNovel 切换、agentMessages 维护），不要尝试抽离。直接写**精简版**，行为约束如下：

- **唯一副作用**：调用 `run.chatState = run.chatState.copyWith(...)` 推进 `streamingSegments` / `messages`。
- **不做**：DB 写、不改 `currentNovel`、不调 `_ref.read(...)`、不读 `currentChatSessionIdProvider`、不触发 UI 滚动。
- **事件→投影规则**（每个事件类型的精确行为）：
  - `TextDeltaEvent(text)` → 把 `text` 追加到 `streamingSegments` 末尾的 `TextSegment`（若末尾是 `TextSegment` 则合并，否则新建 `TextSegment`）。
  - `ToolCallStartEvent(name, args, id)` → 追加 `ToolCallSegment(AgentToolCall(id, name, args, status: running))`。
  - `ToolProgressEvent(id, n)` → 找到 `id` 对应的 `ToolCallSegment`，更新其 `AgentToolCall.progressChars = n`（用 `copyWith`）。
  - `ToolCallEndEvent(name, id, result, ...)` → 找到 `id` 对应的 `ToolCallSegment`，更新 `status` 为 `completed` / `error`，`result` 为截断版。
  - `AgentDoneEvent` → 把 `streamingSegments` 包装为 `AgentChatMessage(role: assistant)` 加入 `messages`，清空 `streamingSegments`。
  - `AgentErrorEvent(error)` → 加入一条 `AgentChatMessage(role: user, segments=[TextSegment('[Agent 错误] $error')])`（仅作详情页可读），清空 `streamingSegments`。
  - `CompactionEvent` → 无操作（子 Agent 不持久化，无需 DB 同步）。

此步骤产出 `subagent_state_projector.dart`（独立文件，class 名字就用 `_SubagentStateProjector`；方法签名 `static void project(AgentEvent event, SubagentRun run)`）。

- [ ] **步骤 6：补 `_buildLlmForScenario` 与 `_executeToolForSubagent`**

`_buildLlmForScenario`：把 `NovelAgentService._buildAgentEnv` 里的 LLM 构造部分抽出为公共方法（或直接在 `SubagentRunner` 注入 `LlmProvider Function(String scenarioId)` 工厂）。子 Agent 复用主 Agent 的 LLM 配置（scenarioId 用 `'writing'` 或新增 `'subagent'`）。

`_executeToolForSubagent`：构造一个 `ToolExecutor`（复用现有 facade）并调其 `execute(name, args, ctx)`。`ctx` 用 `AgentScenarioContext(scenarioId: 'writing', currentNovelId: <从父 session 继承>)`。

> 继承父 session 的 `currentNovelId`：`SubagentRunner.dispatch` 增加可选参数 `parentCurrentNovelId`，由 AgentLoop 改造时从父 scenarioContext 传入。子 Agent 调 `get_outline` 等隐式工具时作用于同一本小说。

- [ ] **步骤 7：补端到端冒烟测试（mock LLM）**

在 `subagent_runner_test.dart` 加：

```dart
test('单个子 Agent 跑完返回 summary（mock LLM 单轮无工具）', () async {
  // 用 mock LlmProvider：一次性返回 "## 最终结论\n完成。"
  // 用真实 registry + forTest=false 的 SubagentRunner
  // 断言 result JSON 含 success:true 且 summary 含「最终结论」
});
```

> 此测试依赖 mock LLM 的现有范式——先 Grep `test/mocks/` 找 `FakeLlmProvider` 或类似，复用。若无，本任务标「手动验证」跳过单测，留集成测试覆盖。

- [ ] **步骤 8：Commit**

```bash
git add novel_app/lib/services/novel_agent/subagent_runner.dart \
        novel_app/lib/services/novel_agent/subagent_state_projector.dart \
        novel_app/test/unit/services/novel_agent/subagent_runner_test.dart
git commit -m "feat(novel_agent): 新增 SubagentRunner（4/30 并发 + runId 打标 + chatState 聚合）"
```

---

## 任务 7：`NovelAgentService` 运行态 key 改 `runId` + `AgentLoop` 工具执行段拆分

**文件：**
- 修改：`lib/services/novel_agent/novel_agent_service.dart`
- 修改：`lib/services/novel_agent/agent_loop.dart`
- 测试：`test/unit/services/novel_agent/agent_loop_subagent_dispatch_test.dart`

**目的：**
（a）`NovelAgentService` 用 `runId` 作运行态 key，支持子 Agent 并行（主+子不互相 `isRunning` 拦截）。
（b）`AgentLoop` 第 5 步「执行工具」拆成「普通工具串行 + dispatch_subagent 并行」。

- [ ] **步骤 1：编写 dispatch 并行测试（失败）**

需要 mock `AgentScenario`（拦截 executeTool）+ mock `LlmProvider`（返回预设 tool_calls）。复用现有测试范式。

```dart
import 'package:flutter_test/flutter_test.dart';
// mock AgentScenario / LlmProvider 的测试见 test/mocks/ 现有 helper

void main() {
  group('AgentLoop dispatch_subagent 并行', () {
    test('同轮 2 个 dispatch_subagent 并行执行（耗时重叠）', () async {
      // mock LLM：第一轮返回 2 个 dispatch_subagent tool_calls
      // mock scenario.executeTool('dispatch_subagent')：sleep 100ms 模拟子 Agent
      // 断言总耗时 < 180ms（并行）而非 ~200ms（串行）
    });

    test('dispatch_subagent 与普通工具混合：普通工具先串行跑完再派子', () async {
      // mock LLM 第一轮返回 [read_chapter, dispatch_subagent]
      // 记录执行顺序：read_chapter 的 end 必须早于 dispatch 的 start
    });
  });
}
```

- [ ] **步骤 2：运行测试验证失败**

运行：`flutter test test/unit/services/novel_agent/agent_loop_subagent_dispatch_test.dart`
预期：FAIL（AgentLoop 仍串行执行工具，并行断言超时）。

- [ ] **步骤 3：改造 `NovelAgentService` 运行态 key**

修改 `novel_agent_service.dart`：

```dart
  /// 按 runId 跟踪运行状态（替代 scenarioId）
  final Map<String, bool> _runningByRunId = {};
  final Map<String, CancellationToken> _tokensByRunId = {};
```

`sendMessage` / `resumeFromMessages` 增加可选参数 `String? runId`：

```dart
  Future<void> sendMessage({
    required String userInput,
    required List<ChatMessage> history,
    required String scenarioId,
    required AgentScenarioContext scenarioContext,
    String? runId, // 新增：主 session 传 sessionId，子 Agent 传独立 runId
  }) async {
    final key = runId ?? scenarioId; // 兼容：未传 runId 时退回 scenarioId
    if (_runningByRunId[key] == true) {
      // ... 同现有 busy 拦截 ...
    }
    _runningByRunId[key] = true;
    final token = CancellationToken();
    _tokensByRunId[key] = token;
    // ... loop.run(emit: ...) emit 时若有 runId 则打标 ...
    // finally: _runningByRunId.remove(key); _tokensByRunId.remove(key);
  }
```

`emit` 回调改造：若 `runId != null`，包装事件打 runId（复用 `_EventTagger`）：

```dart
    emit: runId == null
        ? (event) => _controller.add(event)
        : (event) => _controller.add(_EventTagger.tag(event, runId)),
```

> `_EventTagger` 从 `subagent_runner.dart` 提为顶层公共类（或移到 `agent_event.dart` 旁），供 service 复用。**实现者注意**：把任务 6 的 `_EventTagger` 改为 public `EventTagger` 并移到独立文件 `event_tagger.dart`，service 与 runner 共用。

`cancelFor` / `isRunningFor` 改用 runId key；新增 `cancelForRunId(String runId)`。

- [ ] **步骤 4：改造 `AgentLoop` 工具执行段**

修改 `agent_loop.dart` 第 5 步（当前 `for (final call in toolCalls)` 串行循环）。**关键：`dispatch_subagent` 不走 `_scenario.executeTool`**，而是走注入的 `SubagentRunner.dispatch`。

由于 `AgentLoop` 不应直接依赖 `SubagentRunner`（避免循环依赖），通过 `AgentScenario.executeTool` 转发：主 `WritingScenario` 的 `executeTool` 内部识别 `dispatch_subagent` 并委托 `SubagentRunner`。

**实现路径：**
1. `WritingScenario` 构造函数从 `_ref` 读取 `subagentRunnerProvider`、`currentChatSessionIdProvider`、`agentServiceProvider`（依次参见任务 9 的 Provider），把 `runner`、`parentSessionId` 缓存为字段。
2. `WritingScenario.executeTool` 头部加分支：
   ```dart
   if (name == 'dispatch_subagent') {
     return _subagentRunner.dispatch(
       parentSessionId: _parentSessionId ?? '',
       task: args['task'] as String? ?? '',
       allowedTools: ((args['allowed_tools'] as List?)?.cast<String>()) ?? const [],
       parentCurrentNovelId: _currentContext?.currentNovelId,
       parentToolCallId: <从 executeTool 调用上下文获取，方案见下>,
     );
   }
   ```
3. **父 toolCallId 的传递**：`WritingScenario.executeTool` 当前签名里没有 `toolCallId` 参数。两种处理方案（任选其一，在步骤 5 决定）：
   - 方案 A（推荐）：改 `AgentScenario.executeTool` 接口加可选 `String? toolCallId`，AgentLoop 第 5 步 `_executeSingleTool` 把当前 `call.id` 传下去。改动触达接口签名。
   - 方案 B（侵入最小）：`WritingScenario` 内部维护一个 `String? _pendingToolCallId`，由 `AgentLoop` 第 5 步执行工具前 `scenario.setPendingToolCallId(call.id)` 调用前置赋值（需在 `AgentScenario` 加一个新方法）。不触接口签名。
4. **同步改 `agent_scenario_factory.dart`**：`build()` 在 `case ScenarioIds.writing:` 分支改为 `return WritingScenario(_ref);` 保持不变（WritingScenario 内部从 `_ref` 读 provider）。**不需要改 Factory 构造签名**——Factory 只透传 `_ref`，WritingScenario 内部按需拉 runner。

> 注意：`_scenario.executeTool('dispatch_subagent')` 内部委托 `SubagentRunner.dispatch`，而 runner 内部又起子 `AgentLoop`——这是递归调用链，但子 Agent 的 scenario 是 `SubagentScenario`（不含 dispatch_subagent），所以不会无限递归（强制单层）。

- [ ] **步骤 4a：改造 `AgentLoop` 第 5 步为「普通串行 + dispatch 并行」**

修改 `agent_loop.dart` 第 5 步（当前 `for (final call in toolCalls)` 串行循环）：

```dart
        // 5. 执行工具调用
        // 5a. 先串行执行普通工具
        final subagentCalls = <ToolCall>[];
        for (final call in toolCalls) {
          if (cancellationToken?.isCancelled == true) {
            emit(const AgentDoneEvent());
            return;
          }
          if (call.name == 'dispatch_subagent') {
            subagentCalls.add(call);
            continue; // 收集，稍后并行
          }
          await _executeSingleTool(call, emit); // 抽出现有串行逻辑
        }
        // 5b. 并行执行 dispatch_subagent
        if (subagentCalls.isNotEmpty) {
          await Future.wait(subagentCalls.map((c) => _executeSingleTool(c, emit)));
        }
```

`_executeSingleTool(call, emit)`：把现有第 5 步循环体抽出的私有方法（emit `ToolCallStartEvent` → `_scenario.executeTool`（按步骤 4 的方案 A/B 传 toolCallId）→ JSON 格式化 → append tool 消息 → emit `ToolCallEndEvent`）。普通工具与 dispatch 都走它；并行只是把多个 `await _executeSingleTool` 用 `Future.wait` 并发。

- [ ] **步骤 5：运行测试验证通过**

运行：`flutter test test/unit/services/novel_agent/agent_loop_subagent_dispatch_test.dart`
预期：PASS（并行 + 混合顺序 2 个用例）

- [ ] **步骤 6：运行全量回归**

运行：`flutter test test/unit/services/novel_agent/`
预期：全部 PASS（含现有 agent_loop / scenario_session 测试）

- [ ] **步骤 7：Commit**

```bash
git add novel_app/lib/services/novel_agent/novel_agent_service.dart \
        novel_app/lib/services/novel_agent/agent_loop.dart \
        novel_app/lib/services/novel_agent/event_tagger.dart \
        novel_app/lib/services/novel_agent/scenarios/writing_scenario.dart \
        novel_app/test/unit/services/novel_agent/agent_loop_subagent_dispatch_test.dart
git commit -m "feat(novel_agent): AgentLoop 拆分普通工具串行/dispatch并行 + service 运行态改 runId"
```

---

## 任务 8：`ScenarioSession` 按 runId 过滤事件 + cancel 级联

**文件：**
- 修改：`lib/core/providers/scenario_session.dart`
- 测试：`test/unit/services/agent_launcher/`（现有目录，加 scenario_session_subagent_filter_test.dart）

- [ ] **步骤 1：编写事件过滤测试（失败）**

```dart
// 测试：主 session 收到 runId=null 与 runId=本session 的事件 → 处理
//       收到 runId=子run 的事件 → 忽略（不污染主对话）
```

- [ ] **步骤 2：运行验证失败**

- [ ] **步骤 3：实现过滤**

修改 `scenario_session.dart` 的 `_handleAgentEvent`：

```dart
  void _handleAgentEvent(AgentEvent event) {
    // 子 Agent 事件：主 session 直接忽略（由 SubagentRun 自己的订阅消费）
    final runId = event.runId;
    if (runId != null && !_isOwnMainRunId(runId) && !_isOwnSubagentRun(runId)) {
      return;
    }
    // 子 Agent 事件即使属于本 session 派出的，也不进主对话历史
    if (runId != null && runId != _mainRunId) {
      // 仅触发主气泡卡片刷新（通过 registry 通知），不更新主 messages
      _notifySubagentProgress(runId);
      return;
    }
    // ... 现有主 Agent 事件处理 ...
  }
```

`_isOwnSubagentRun`：查 `SubagentRegistry` 是否有此 runId 且 `parentSessionId == 本 session`。
`_mainRunId`：主 Agent 的 runId（= sessionId 或 sendMessage 时传入）。

cancel 级联：`ScenarioSession.cancel()` 内追加：

```dart
    // 级联取消本 session 派出的所有活跃子 Agent
    _subagentRunner?.cancelAllForSession(sessionId);
```

- [ ] **步骤 4：运行测试验证通过**

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/core/providers/scenario_session.dart \
        novel_app/test/unit/services/agent_launcher/scenario_session_subagent_filter_test.dart
git commit -m "feat(novel_agent): ScenarioSession 按 runId 过滤子 Agent 事件 + cancel 级联"
```

---

## 任务 9：Provider 层（registry / runner / run 查询）

**文件：**
- 创建：`lib/core/providers/subagent_providers.dart`
- 测试：`test/unit/providers/subagent_providers_test.dart`

- [ ] **步骤 1：编写测试**

```dart
// subagentRegistryProvider 单例
// subagentRunProvider((sessionId, runId)) 返回对应 run
// currentSubagentRunsProvider 返回当前 session 的 run 列表
```

- [ ] **步骤 2-4：失败 → 实现 → 通过**

创建 `lib/core/providers/subagent_providers.dart`：

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../services/novel_agent/subagent_registry.dart';
import '../../services/novel_agent/subagent_runner.dart';
import '../../services/novel_agent/novel_agent_service.dart';
import 'scenario_sessions_provider.dart'; // 取 currentSessionId

final subagentRegistryProvider = Provider<SubagentRegistry>((ref) {
  return SubagentRegistry();
});

final subagentRunnerProvider = Provider<SubagentRunner>((ref) {
  final registry = ref.watch(subagentRegistryProvider);
  final agentService = ref.watch(novelAgentServiceProvider);
  return SubagentRunner(ref: ref, registry: registry, agentService: agentService);
});

final subagentRunProvider = Provider.family<SubagentRun?, (String, String)>(
  (ref, pair) {
    final registry = ref.watch(subagentRegistryProvider);
    return registry.get(pair.$1, pair.$2);
  },
);

final currentSubagentRunsProvider = Provider<List<SubagentRun>>((ref) {
  final registry = ref.watch(subagentRegistryProvider);
  final sessionId = ref.watch(currentChatSessionIdProvider);
  if (sessionId == null) return const [];
  return registry.listForSession(sessionId);
});
```

> `currentChatSessionIdProvider` 位于 `lib/core/providers/chat_session_providers.dart:21`（`StateProvider<int?>`，null 表示未选会话）。返回 `int?` 的 sessionId——`SubagentRegistry` 用 String 作 parentSessionId key，故此处需 `.toString()`。若主 Agent 实际用 scenarioId 作 parentSessionId，则改用 `currentAgentScenarioProvider`（`scenario_sessions_provider.dart:242`）。实现者在步骤 1 先确认主 session 的 parentSessionId 到底是 sessionId 还是 scenarioId，再统一口径（任务 8 `_isOwnMainRunId` 也依赖同一口径）。

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/core/providers/subagent_providers.dart \
        novel_app/test/unit/providers/subagent_providers_test.dart
git commit -m "feat(novel_agent): 新增 subagent 相关 Provider"
```

---

## 任务 10：UI — `SubagentToolCard`（主气泡卡片）

**文件：**
- 创建：`lib/widgets/agent_chat/subagent_tool_card.dart`
- 修改：`lib/widgets/agent_chat/agent_message_bubble.dart`
- 测试：`test/widgets/agent_chat/subagent_tool_card_test.dart`

- [ ] **步骤 1：编写 Widget 测试**

```dart
// 四种状态渲染：pending / running / completed / failed
// 点击触发 onTap 回调
```

- [ ] **步骤 2-4：失败 → 实现 → 通过**

`subagent_tool_card.dart`：ConsumerWidget，watch `subagentRunProvider((sessionId, runId))`，按 `run.state` 渲染不同样式 + 进度摘要 + 可点击。

`agent_message_bubble.dart`：在渲染 `ToolCallSegment` 时，若 `segment.toolName == 'dispatch_subagent'`，从 `segment.arguments` 取 `task`，用 `runId`（需从 ToolCallStart 关联——见下）渲染 `SubagentToolCard` 替代普通工具卡片。

> runId 关联：`dispatch_subagent` 的 `ToolCallStartEvent` 在主 session 里正常 emit（由 WritingScenario.executeTool 内部 emit）。runId 从 toolCallId 映射——SubagentRunner.dispatch 创建 run 后，把 `runId` 通过 `ToolCallEndEvent` 的 fullResult 携带回主 session。UI 用 `toolCallId` 反查 run。**实现者需在 WritingScenario 委托 dispatch 时，emit 带主 session runId 的 ToolCallStart/End，并把子 runId 放进 result JSON**。

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/widgets/agent_chat/subagent_tool_card.dart \
        novel_app/lib/widgets/agent_chat/agent_message_bubble.dart \
        novel_app/test/widgets/agent_chat/subagent_tool_card_test.dart
git commit -m "feat(novel_agent): 主气泡新增 SubagentToolCard 子任务卡片"
```

---

## 任务 11：UI — `SubagentDetailScreen`（详情页）

**文件：**
- 创建：`lib/screens/subagent_detail_screen.dart`
- 测试：`test/screens/subagent_detail_screen_test.dart`

- [ ] **步骤 1：编写 Widget 测试**

```dart
// 显示 task / allowedTools chips / 状态
// 只读渲染 chatState.messages + streamingSegments
// running 时显示停止按钮，点击触发 cancel
// 返回键不取消（验证不调用 cancel）
```

- [ ] **步骤 2-4：失败 → 实现 → 通过**

`subagent_detail_screen.dart`：ConsumerWidget，接收 `sessionId` + `runId`，watch `subagentRunProvider`。布局：
- 顶部信息：任务标题（task）、allowedTools chips、状态标签（`run.state.name`）、运行时长（`DateTime.now() - run.createdAt`，每 1 秒刷新一次）。
- 消息列表：**不直接复用 `agent_chat_dialog.dart`**（那是完整 Dialog，含输入框/会话切换）。**实现方案**（任选其一，由实现者在步骤 2 决定）：
  - 方案 A：抽出 `agent_chat_dialog.dart` 的消息列表渲染段（`ListView.builder` 遍历 `state.messages + streamingSegments`，每条走 `ChatHistoryListItem`），写成 `lib/widgets/agent_chat/agent_chat_message_list.dart` 公共组件，详情页与 dialog 共用。
  - 方案 B（侵入更小）：在 `SubagentDetailScreen` 内直接写一个简化版 `ListView`（同上遍历，单条渲染 `ChatHistoryListItem`），不复用、不抽组件。代码 ~30 行。
- 底部停止按钮：仅当 `run.state == SubagentRunState.running || run.state == SubagentRunState.pending` 时显示；点击调 `run.tokenSource?.cancel(reason: '用户主动停止')`。

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/screens/subagent_detail_screen.dart \
        novel_app/test/screens/subagent_detail_screen_test.dart
git commit -m "feat(novel_agent): 新增 SubagentDetailScreen 子 Agent 详情页"
```

---

## 任务 12：路由接入 + 端到端手动验证

**文件：**
- 修改：`lib/main.dart`（路由）或 `agent_message_bubble.dart`（直接 Navigator.push）

- [ ] **步骤 1：卡片点击跳转详情页**

在 `SubagentToolCard` 的 onTap 里：

```dart
Navigator.of(context).push(MaterialPageRoute(
  builder: (_) => SubagentDetailScreen(
    sessionId: sessionId,
    runId: runId,
  ),
));
```

- [ ] **步骤 2：flutter analyze**

运行：`cd novel_app && flutter analyze lib/services/novel_agent/ lib/core/providers/ lib/widgets/agent_chat/ lib/screens/subagent_detail_screen.dart`
预期：无 error

- [ ] **步骤 3：全量测试**

运行：`cd novel_app && flutter test`
预期：全部 PASS

- [ ] **步骤 4：手动验证（真机/模拟器）**

启动 app，在 Agent Chat 里发一条让主 Agent 派子 Agent 的请求（如「请派一个子 Agent 读取大纲并总结」），验证：
- 主气泡出现子任务卡片
- 卡片显示进度
- 点击进入详情页看到子 Agent 流式对话
- 详情页返回后子 Agent 继续
- 子 Agent 完成后主 Agent 拿到总结继续

- [ ] **步骤 5：Commit**

```bash
git add novel_app/lib/widgets/agent_chat/subagent_tool_card.dart
git commit -m "feat(novel_agent): SubagentToolCard 点击跳转详情页 + 接入完成"
```

---

## 验收清单

- [ ] `AgentEvent.runId` 非破坏性引入，全量测试通过
- [ ] `SubagentRun` / `SubagentRegistry` 内存模型 + 单测
- [ ] `dispatch_subagent` 工具注册 + `filterTools` 白名单（强制无嵌套）
- [ ] `SubagentScenario` 白名单过滤 + 通用 prompt
- [ ] `SubagentRunner` 4/30 并发 + runId 打标 + chatState 聚合
- [ ] `AgentLoop` 普通工具串行 / dispatch 并行
- [ ] `NovelAgentService` 运行态改 runId（主子并行不互斥）
- [ ] `ScenarioSession` 按 runId 过滤 + cancel 级联
- [ ] Provider 层（registry / runner / run 查询）
- [ ] `SubagentToolCard` 主气泡卡片（四态 + 可点）
- [ ] `SubagentDetailScreen` 只读详情页 + 停止按钮
- [ ] flutter analyze 无 error，flutter test 全绿
- [ ] 手动端到端验证通过

## 风险与回退

- **递归风险**：子 Agent 调 dispatch_subagent → `SubagentScenario.tools` 不含它 + `executeTool` 双重拒绝，理论不可能。若线上出现，SubagentScenario 的拒绝兜底。
- **事件串扰**：若某处漏了 runId 过滤，主对话会被子 Agent 文本污染。回归测试 `scenario_session_subagent_filter_test.dart` 守护。
- **并发死锁**：`_waitForSlot` 用轮询 50ms，若所有子 Agent 卡死不释放，主 Agent 会一直等。缓解：子 Agent 有 `llmStreamTimeout`（5min）+ maxRounds，不会永久卡；主 Agent cancel 会级联取消。
- **回退**：每个任务独立 commit，可按任务回退。最坏情况保留任务 1-5（纯新增模型/工具，无副作用），回退任务 6-12（运行时与 UI）。
